const Collection = require('./collection.constant');
const User = require('./user.constant');
const Common = require('./common.constant');
const Job = require('./job.constant');

module.exports = {
  Collection,
  User,
  Common,
  Job,
};
